extends layout

block content
    .container
        h1 #{title}
        form(method="POST", action="/articles/add")
            #form-group
                label Title:
                input.form-control(name='title', type=text)
            //- hr
            #form-group
                label Content:
                br
                textarea.form-control#text-editor(name='body')
            br
            input.btn.btn-primary(type="submit", value="submit")